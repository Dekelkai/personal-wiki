# 个人 Wiki 项目总览与路线图

> 本文档是 personal-wiki 的长期项目上下文，用于记录项目目标、知识分类、对象边界、维护原则和阶段路线。后续开始较大任务前，应先阅读本文档、`AGENTS.md` 和 `README.md`。

最后更新：2026-07-17

## 1. 项目定位

personal-wiki 是一个面向长期维护的个人知识系统，用于组织、关联、检索和复用科研知识、计算机知识、工程经验、AI 实践、项目记录和写作成果。

正式知识源是：

- Markdown；
- YAML Frontmatter；
- Git 历史。

Obsidian 是主要编辑工具，Quartz 是公开展示层，GitHub Pages 是当前部署渠道。Quartz 不是知识库本体，不应反向决定知识对象如何划分。

本项目不是：

- PaperNotes 的公开镜像；
- Zotero 元数据或 PDF 的复制仓库；
- AI 对话、命令输出和实验草稿的集中归档；
- Hexo 博客的双向同步副本；
- 为 MCP、向量数据库或 RAG 单独建设的数据集。

## 2. 内容来源与职责边界

### 原始资料

原始资料包括 PaperNotes、Zotero PDF 与批注、临时想法、AI 对话、实验草稿、故障过程、博客原稿和项目工作日志。

这些材料只作为输入和证据，不直接原样复制为正式 Wiki 页面。

### 外部来源

- `F:\OneDrive\obsidian\PaperNotes`：私人论文阅读笔记，只允许选择性读取和提炼，禁止 Agent 修改或批量复制。
- `F:\OneDrive\obsidian\Notes`：专题报告、课程或技术笔记等原始材料。用户提供明确文件路径后只读分析，禁止 Agent 修改或无边界扫描。
- Zotero：负责论文元数据、PDF、CiteKey、DOI、批注和文献来源。
- `F:\Blog`：Hexo 博客，负责已发布或准备发布的叙事型文章，禁止 Agent 修改。

### 规范知识库

`F:\KnowledgeBase\personal-wiki` 是整理后的正式知识库。Agent 可以在治理规则约束下修改该仓库，但不得因为页面设置了 `publish: false` 就把敏感原始材料写入仓库。

## 3. 知识分类原则

分类同时区分稳定领域、知识对象和交叉主题，不能只依赖一棵无限加深的目录树。

### 3.1 稳定领域：页面主要放在哪里

```text
知识首页
├── 计算机科学
│   ├── 算法与数据结构
│   ├── 编程语言
│   ├── 计算机系统
│   ├── 软件工程
│   └── 人工智能
├── 研究资料
│   ├── 论文
│   ├── 数据集与基准
│   ├── 评价指标
│   ├── 综述与报告
│   └── 参考资料
├── 实践记录
│   ├── 项目记录
│   ├── 实验记录
│   ├── 技术决策
│   └── 问题排查
└── 写作与输出
```

时间与进度不作为知识目录。“当前研究”“最近更新”“进行中的项目”和“已完成项目”等信息只保留在 Frontmatter、具体页面或归档中。

### 3.2 知识对象：页面本身是什么

核心对象类型包括：

| 类型 | 职责 |
| --- | --- |
| `index` | 组织领域入口和页面关系，不承担具体知识解释 |
| `concept` | 解释稳定概念、术语和基本原理 |
| `topic` | 定义研究主题、研究问题、任务边界和开放问题 |
| `method` | 说明方法目标、输入输出、假设、步骤、实现和局限 |
| `dataset` | 记录数据来源、许可、版本、划分、标注和适用任务 |
| `metric` | 说明评价指标的定义、计算、方向、聚合和比较限制 |
| `paper` | 保存论文元数据、作者贡献、报告结果、证据和局限 |
| `survey` | 按明确范围、纳入标准和证据组织多份来源 |
| `experiment` | 记录用户实际执行的实验、配置、结果和误差分析 |
| `project` | 组织项目目标、范围、里程碑、资源和当前状态 |
| `decision` | 记录实际决策的背景、候选方案、权衡和后续影响 |
| `guide` | 保存经过验证、可复用的操作流程 |
| `troubleshooting` | 保存问题现象、原因、排查过程、修复和验证方法 |
| `reference` | 保存命令、清单、术语和资源等快速参考内容 |
| `article` | 组织面向外部读者的完整叙事型文章或博客索引 |

目录表达主要领域，Frontmatter `type` 表达对象性质，`topics` 表达计算机视觉、视觉定位、Go 等交叉主题。页面只保留一个规范位置，通过 Wiki Link 与其他领域建立关系。

### 3.3 对象到目录的默认映射

| 对象 | 默认目录 |
| --- | --- |
| `concept`、`topic`、`method` | `content/computer-science/` |
| `paper`、`dataset`、`metric`、`survey`、`reference` | `content/resources/` |
| `experiment`、`project`、`decision`、`troubleshooting` | `content/practice/` |
| `article` | `content/writing/` |
| `index`、`guide` | 根据页面实际职责选择相应稳定领域 |

## 4. 计算机科学分类

计算机科学用于承载稳定的学科知识和工程方法。

```text
计算机科学
├── 算法与数据结构
├── 编程语言
│   ├── Go
│   ├── Python
│   └── C/C++
├── 计算机系统
│   ├── 操作系统
│   ├── 计算机网络
│   ├── 数据库
│   └── 分布式系统
├── 软件工程
└── 人工智能
```

### 算法与数据结构的对象划分

| 内容 | 推荐类型 | 示例 |
| --- | --- | --- |
| 数据结构定义 | `concept` | 数组、链表、哈希表、树、图、堆 |
| 算法范式 | `concept` 或 `method` | 分治、贪心、动态规划、回溯 |
| 具体算法 | `method` | 二分查找、Dijkstra、KMP、归并排序 |
| 复杂度知识 | `concept` | 时间复杂度、空间复杂度、均摊分析 |
| 语言实现经验 | `guide` | Go 中实现堆、Python 图遍历注意事项 |
| 性能比较 | `experiment` | 不同算法或实现的基准测试 |

单道算法题和临时解题过程属于原始学习材料。只有可复用的概念、方法和实现经验进入正式 Wiki。

## 5. 人工智能与视觉定位

人工智能需要区分学科知识与工具实践。

```text
人工智能
├── AI 基础
│   ├── 机器学习
│   ├── 深度学习
│   ├── 表征学习
│   └── 模型评测
├── 计算机视觉
│   ├── 图像分类
│   ├── 目标检测与分割
│   ├── 图像检索
│   ├── 特征匹配与配准
│   ├── 视觉定位
│   └── 目标跟踪
├── 自然语言处理
├── 基础模型与多模态
│   ├── Transformer
│   ├── 大语言模型
│   ├── 视觉语言模型
│   └── 多模态学习
└── AI 工具与实践
    ├── AI Agent
    ├── 提示词与上下文
    ├── MCP
    ├── Codex CLI
    ├── Claude Code
    ├── AI 辅助科研
    └── AI 辅助开发
```

Codex、Claude Code、MCP 和提示词属于 AI 工具与实践，不与计算机视觉、自然语言处理等研究领域视为同层学科。

大语言模型与自然语言处理关系密切，多模态模型同时连接计算机视觉，因此采用一个主要归属和多个主题链接，不强制构造互斥目录树。

现有无人机视觉定位内容的稳定路径是：

```text
计算机科学
└── 人工智能
    └── 计算机视觉
        └── 视觉定位
            └── 无人机绝对视觉定位
```

对应论文、数据集、指标和综述进入研究资料；用户实际实验和项目进入实践记录。目标检测、分割、自然语言处理和大模型等方向只有形成实质内容时才创建页面。

## 6. 研究资料与实践记录

研究资料回答“结论和比较依赖哪些来源”，实践记录回答“用户实际执行和验证了什么”。

- 论文页面保存单一来源的元数据、作者结论和局限；
- 数据集与指标页面保存版本、许可、协议和可比条件；
- 综述页面综合多份来源；
- 实验页面保存用户实际配置、结果和失败分析；
- 项目和决策页面保存目标、范围、权衡与影响。

## 7. 原始资料到正式知识的流程

处理任意论文、笔记、实验或项目材料时，遵循以下流程：

1. 识别来源性质和可信范围；
2. 搜索现有标题、别名和近义概念；
3. 识别其中的 topic、concept、method、dataset、metric、paper、experiment 等对象；
4. 优先更新已有页面；
5. 只有对象边界稳定且具有实质内容时才创建页面；
6. 区分外部事实、论文作者结论、用户实验结果、用户判断和 Agent 推断；
7. 为外部事实和论文结论保留来源；
8. 将实验和决策连接到具体项目；
9. 将成熟对象连接到相关领域页；
10. 默认保持 `publish: false`，公开状态由人工确认。

正式知识的目标不是重现原始笔记，而是形成可由多份来源持续补充的知识对象。

## 8. 公开与私有边界

- `publish: false` 只控制 Quartz 是否生成页面，不是敏感信息保护机制；
- 仓库中不得保存 API Key、Token、密码、Cookie、私钥、私人服务器地址、未公开论文、组内资料或敏感实验数据；
- `_inbox`、`_templates` 和 `_archive` 不进入公开网站；
- 公开页面中的 Wiki Link 原则上只能指向同样公开的页面；
- 未公开目标在正式公开页面中先使用普通文本，成熟并发布后再转换为 Wiki Link；
- Agent 不得自动修改 `publish`。

## 9. 当前阶段

### 已完成

- 仓库和 Obsidian Vault 骨架；
- 基础领域入口；
- 模板、Frontmatter Schema 和检查脚本；
- Agent 基础规则；
- Quartz v5、ExplicitPublish、GitHub Actions 和 GitHub Pages；
- 公开页面语言整理和单 H1 处理；
- 私有内容建设计划；
- 内容与排版规范和可迭代的材料提炼 Skill；
- `topic`、`metric`、`survey` 类型及对应模板与检查支持；
- 无人机绝对视觉定位首批私有知识对象：1 个主题、1 个数据集综述、3 个数据集和 1 个指标页面；
- 使用临时内容副本的本地私有页面预览脚本；
- 无姓名的公开站点标题和五个简化领域入口；
- 公开站点隐藏内部维护规范和更新记录，并使用中文目录树组织公开页面；

### 尚未完成

- 真实的 paper、method 和 experiment 页面，以及更多可复用的方法与指标内容；
- 来源与引用规范；
- 首批数据集知识页面的人工审阅与修订；
- Zotero 最小接入；
- 博客关联；
- 公开页面到私有页面的链接治理；
- Schema、模板和检查脚本的一致性；
- 长期内容维护机制。

### 当前不应优先开始

- 批量导入 PaperNotes；
- 自动拆分数百篇论文；
- MCP Server；
- 向量数据库；
- RAG 和智能问答。

## 10. 阶段路线

### 阶段 0：规范对齐

目标：让项目说明、分类、Schema、模板、检查脚本和公开页面遵守同一套规则。

主要任务：

1. 建立计算机科学、研究资料、实践记录和写作与输出四个稳定顶层领域；
2. 将人工智能归入计算机科学，并建立计算机视觉和视觉定位层级；
3. 使用 `domain` 表达稳定领域、`type` 表达知识对象、`topics` 表达交叉主题；
4. 将论文、数据集、指标和综述迁移到资源目录；
5. 将实验、项目、决策和问题排查迁移到实践目录；
6. 移除时间和进度型公开导航；
7. 更新 README、AGENTS、模板、Schema、预览脚本和说明文档；
8. 建立内容与 Quartz 排版规范，以及可迭代的材料提炼 Skill。

完成标准：项目规则不存在明显冲突，公开页面没有指向未发布目标的链接，后续 Agent 能根据文档正确判断知识对象和存放位置。

### 阶段 1：小规模研究知识提炼试点

试点结果：已用一份无人机视觉定位数据集报告形成 topic、survey、dataset 和 metric 私有页面，并保留报告位置与官方来源。后续材料继续用于验证 paper、method、experiment 等对象边界。

建议输入：

- 3 篇围绕无人机跨视角绝对视觉定位的论文；
- 1 份数据集官方说明；
- 1 份用户已有实验或复现记录。

预期输出：

- 1 个研究主题；
- 3 个论文证据页；
- 2 至 3 个方法页；
- 1 个数据集页；
- 1 至 2 个指标页；
- 1 个实验页。

完成标准：每个综合结论可回到来源，论文报告与用户实验清楚分离，第二批材料能够复用同一处理流程。

### 阶段 2：固化知识模型与 Zotero 最小接入

根据试点结果调整 Schema、模板、命名和来源字段，建立 CiteKey、DOI 和 Zotero 元数据的最小连接。此阶段只解决来源追踪，不自动复制 PDF、批注或完整笔记。

### 阶段 3：小批量扩展真实内容

每批处理少量相关材料，逐渐形成方法、数据集、指标、综述和实验网络。领域页只链接已经存在且具有实质内容的页面。

### 阶段 4：项目、实验与写作闭环

将研究主题连接到实际项目、实验、技术决策、论文写作和博客输出，验证知识库能否支持真实研究工作。

### 阶段 5：选择性自动化

只有当手工流程稳定后，才实现元数据同步、候选对象提取、选择性导入和自动检查。所有内容拆分、事实判断和公开状态仍需人工确认。

### 阶段 6：检索、MCP 与 RAG

只有当知识库已经包含足够的真实、结构化、可追踪内容，并且存在明确的检索与问答需求后，才评估 MCP、全文检索增强、向量检索和 RAG。

## 11. 当前试点

在继续批量建设内容前，先使用一份真实但范围有限的材料验证提炼流程：

- 输入：用户明确指定的《无人机视觉定位数据集分类与规模报告_更新版》DOCX 与 PDF；
- 模式：先只读分析，经人工确认后创建私有正式页面；
- 目标：识别报告中的数据集、分类维度、可核验事实、来源缺口和候选知识对象；
- 输出：材料分析、候选页面及更新建议、人工确认项和 Skill 缺口复盘；
- 限制：不把完整报告、长段摘录或截图复制进仓库。

该试点完成后，再决定数据集页面、研究主题页面、来源字段和模板需要怎样调整。Go 基础语法或算法与数据结构可作为第二类非科研材料试点，用于验证同一 Skill 是否能正确处理技术学习内容，而不是先批量创建页面。

当前进度：

- 已交叉核对 DOCX 结构与 8 页 PDF 版面；表 3-1 至表 6-1 包含 27 个分类条目和 26 个去重后的命名对象，报告封面的“25 项”与明细不一致；
- 第二阶段已选择 AerialVL、OrthoLoC 和 JointLoc dataset 进行官方来源核验；
- 已完成 `topic`、`metric`、`survey` 的 Schema、模板和检查脚本对齐；
- 页面设计与实施结果记录在 `docs/pilots/无人机视觉定位数据集试点方案.md`；
- 已创建 1 个主题、1 个覆盖完整报告分类的综述、3 个独立数据集页面和 1 个指标页面；
- JointLoc 作为行星场景相邻分支，独立论文页等待 Zotero/CiteKey 规范确定。

## 12. 材料提炼与 Skill

仓库级 Skill 位于 `.agents/skills/personal-wiki-curator/`。它用于处理用户明确指定的论文、PDF、笔记、实验记录和技术材料。

Skill 的默认行为是：

1. 阅读项目规则与当前 Schema；
2. 只读检查材料并保留来源定位；
3. 搜索已有知识对象；
4. 提出更新或新建建议；
5. 等待人工确认后再修改正式内容；
6. 修改时备份并运行检查与构建；
7. 任务末尾复盘 Skill 是否遗漏了可复用规则。

Skill 不是一成不变的。安全、事实边界和重复出现的流程问题应逐步固化；一次性措辞和审美偏好不应立即写成永久规则。Skill 修改必须可审查，并说明原因与适用范围。

## 13. 下一步

当前顺序是先在精简后的本地研究预览中人工审阅首批页面，再根据反馈修订对象边界、来源表达和页面结构。公开入口、README 和旧链接名称已经完成同步整理。

实施时应拆成小提交，建议顺序为：

1. 人工审阅无人机视觉定位首批私有页面；
2. 根据审阅结果调整数据集、综述、指标和主题页边界；
3. 确定 Zotero/CiteKey 与独立论文页的最小规范；
4. 使用少量论文和用户实验验证 `paper`、`method`、`experiment`；
5. 再用少量 Go 或算法材料验证跨领域流程；
6. 流程稳定后才考虑选择性自动化。

## 14. 长期维护规则

- 先搜索，再新建；
- 先识别对象，再选择目录；
- 一个页面只有一个主要职责；
- 不创建只有标题和待补充项的正式页面；
- 不把论文页面当作全部知识的容器；
- 不把项目进展、实验事实、推测和外部结论混在一起；
- 不用目录树表达所有关系；
- 通过 Wiki Link 连接跨领域对象；
- 重要结论保留来源；
- 修改正式内容前备份；
- 默认不删除、不批量移动、不修改发布状态；
- 修改后运行检查和 Quartz 构建；
- 处理外部材料时优先使用 `$personal-wiki-curator`，先分析后应用；
- 每次材料任务结束后做 Skill 缺口复盘，只有可复用问题才固化；
- 未经明确要求不得 push；
- 不修改 PaperNotes、Notes 和 Blog。
